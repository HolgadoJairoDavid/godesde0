package main

import (
	"github.com/HolgadoJairoDavid/godesde0/files"
)

func main() {
	/* estado, texto := variables.ConviertoATexto(15)
	fmt.Println(estado, texto)
	if os := runtime.GOOS; os == "linux" || os == "OS X." {
		fmt.Println("Esto no es window")
	} else {
		fmt.Println("Esto es window")

	}

	switch os := runtime.GOOS; os {
	case "linux":
		fmt.Println("Esto es linux")
	default:
		fmt.Printf("%s \n", os)
	} */
	// number, text := ejercicios.PrimerEjercicio("56")
	// fmt.Println(number, text)
	// teclado.IngresoNumeros()
	// iteraciones.Iterar()
	// ejercicios.TablaDeMultiplicar()
	// files.GrabaTabla()
	// files.SumaTabla()
	// files.LeoArchivos()
	files.LeoArchivos2()
}
